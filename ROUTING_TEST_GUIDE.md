# 클라이언트 사이드 히스토리 라우팅 테스트 가이드

이 문서는 구현된 클라이언트 사이드 히스토리 라우팅 기능을 수동으로 테스트하는 방법을 설명합니다.

## 테스트 환경 준비

### 로컬 테스트
```bash
# 프로젝트 빌드
npm run build:web

# 로컬 서버 실행
cd web-build
python3 -m http.server 8080
```

브라우저에서 http://localhost:8080 접속

### GitHub Pages 테스트
https://trollgameskr.github.io/talk-practice/ 접속

## 테스트 케이스

### 1. 기본 네비게이션 테스트

**목적**: 각 화면으로 이동 시 URL이 올바르게 변경되는지 확인

**절차**:
1. 홈 화면 접속 (URL: `/` 또는 `/talk-practice/`)
2. "Start Practice" 버튼 클릭
3. URL이 `/topics` (또는 `/talk-practice/topics`)로 변경되는지 확인
4. 주제 선택 (예: Daily)
5. URL이 `/conversation/daily` (또는 `/talk-practice/conversation/daily`)로 변경되는지 확인
6. "Progress" 메뉴 클릭
7. URL이 `/progress` (또는 `/talk-practice/progress`)로 변경되는지 확인
8. "Settings" 메뉴 클릭
9. URL이 `/settings` (또는 `/talk-practice/settings`)로 변경되는지 확인

**예상 결과**: 
- 각 화면 전환 시 URL이 올바르게 업데이트됨
- 브라우저 주소창에 현재 화면에 맞는 경로 표시

### 2. 브라우저 뒤로가기 테스트

**목적**: 브라우저 뒤로가기 버튼이 올바르게 작동하는지 확인

**절차**:
1. 홈 → 주제 선택 → 대화 화면 순서로 이동
2. 브라우저 뒤로가기 버튼 (←) 클릭
3. 주제 선택 화면으로 돌아가는지 확인
4. 다시 뒤로가기 클릭
5. 홈 화면으로 돌아가는지 확인
6. 다시 뒤로가기 클릭
7. 외부 사이트로 이탈하지 않고 홈 화면에 머무는지 확인

**예상 결과**:
- 뒤로가기 시 이전 화면으로 정상 이동
- 홈 화면에서 뒤로가기 시 앱 내에 머물러 있음 (외부 사이트로 이탈하지 않음)

### 3. 안드로이드 뒤로가기 제스처 테스트 (모바일만 해당)

**목적**: 안드로이드 뒤로가기 제스처가 올바르게 작동하는지 확인

**절차**:
1. 안드로이드 기기의 Chrome 브라우저에서 앱 접속
2. 홈 → 주제 선택 → 대화 화면 순서로 이동
3. 안드로이드 뒤로가기 제스처 (화면 왼쪽 가장자리에서 오른쪽으로 스와이프) 또는 뒤로가기 버튼 사용
4. 주제 선택 화면으로 돌아가는지 확인
5. 계속 뒤로가기하여 홈 화면까지 이동

**예상 결과**:
- 뒤로가기 제스처/버튼 시 이전 화면으로 정상 이동
- 홈 화면에서 뒤로가기 시 앱 내에 머물러 있음

### 4. 직접 URL 접근 테스트

**목적**: 특정 화면의 URL을 직접 입력했을 때 올바른 화면이 표시되는지 확인

**절차**:
1. 브라우저 주소창에 다음 URL 직접 입력 (GitHub Pages의 경우):
   - `https://trollgameskr.github.io/talk-practice/topics`
   - `https://trollgameskr.github.io/talk-practice/progress`
   - `https://trollgameskr.github.io/talk-practice/settings`
   - `https://trollgameskr.github.io/talk-practice/conversation/daily`

2. 각 URL 입력 시 해당 화면이 직접 표시되는지 확인

**예상 결과**:
- 각 URL 접근 시 404 오류 없이 해당 화면 직접 표시
- 네비게이션 스택이 올바르게 구성됨

### 5. 페이지 새로고침 테스트

**목적**: 특정 화면에서 페이지를 새로고침했을 때 상태가 유지되는지 확인

**절차**:
1. 주제 선택 화면으로 이동
2. F5 또는 Ctrl+R (Mac: Cmd+R)로 페이지 새로고침
3. 주제 선택 화면이 유지되는지 확인
4. 설정 화면으로 이동
5. 페이지 새로고침
6. 설정 화면이 유지되는지 확인

**예상 결과**:
- 새로고침 후에도 현재 화면 유지
- 404 오류 발생하지 않음

### 6. URL 북마크 테스트

**목적**: URL을 북마크하고 다시 방문했을 때 정상 작동하는지 확인

**절차**:
1. 주제 선택 화면 접속 (`/topics`)
2. 브라우저 북마크에 추가 (Ctrl+D 또는 Cmd+D)
3. 홈 화면으로 이동
4. 북마크한 주제 선택 화면 클릭
5. 주제 선택 화면으로 직접 이동하는지 확인

**예상 결과**:
- 북마크를 통해 특정 화면에 직접 접근 가능
- 404 오류 없이 정상 표시

### 7. 앞으로가기 테스트

**목적**: 뒤로가기 후 앞으로가기가 올바르게 작동하는지 확인

**절차**:
1. 홈 → 주제 선택 → 진도 확인 순서로 이동
2. 브라우저 뒤로가기 2번 클릭 (홈 화면까지)
3. 브라우저 앞으로가기 버튼 (→) 클릭
4. 주제 선택 화면으로 이동하는지 확인
5. 다시 앞으로가기 클릭
6. 진도 확인 화면으로 이동하는지 확인

**예상 결과**:
- 앞으로가기 시 올바른 순서로 화면 이동
- URL이 각 화면에 맞게 변경됨

### 8. 연속 네비게이션 테스트

**목적**: 여러 화면을 빠르게 이동한 후 뒤로가기가 올바르게 작동하는지 확인

**절차**:
1. 홈 → 주제 선택 → 대화 → 홈 → 진도 → 설정 → 외관 설정 순서로 빠르게 이동
2. 뒤로가기를 여러 번 클릭하여 역순으로 이동
3. 각 단계에서 올바른 화면이 표시되는지 확인

**예상 결과**:
- 모든 네비게이션 기록이 정확히 저장됨
- 뒤로가기 시 이동 순서의 역순으로 정확히 이동

## 통과 기준

모든 테스트 케이스에서:
- ✅ 404 오류 발생하지 않음
- ✅ URL과 화면이 항상 동기화됨
- ✅ 뒤로가기/앞으로가기가 예상대로 작동
- ✅ 외부 사이트로 의도치 않게 이탈하지 않음
- ✅ 새로고침 시 현재 화면 유지
- ✅ 직접 URL 접근 정상 작동

## 문제 발생 시 확인사항

1. **404 오류 발생**:
   - `public/404.html` 파일이 배포되었는지 확인
   - GitHub Pages 설정에서 Build and deployment 소스가 "GitHub Actions"인지 확인

2. **뒤로가기가 작동하지 않음**:
   - 브라우저 콘솔에서 JavaScript 오류 확인
   - React Navigation linking 설정이 올바른지 확인

3. **URL이 업데이트되지 않음**:
   - NavigationContainer의 linking prop이 설정되어 있는지 확인
   - 웹 환경인지 확인 (Platform.OS === 'web')

4. **새로고침 시 홈으로 이동**:
   - `index.html`의 SPA 라우팅 스크립트가 실행되는지 확인
   - sessionStorage에 redirectPath가 저장되는지 확인

## 추가 테스트 (선택사항)

### 성능 테스트
- 네트워크 탭에서 페이지 로드 시간 확인
- 뒤로가기/앞으로가기 시 불필요한 네트워크 요청이 없는지 확인

### 접근성 테스트
- 키보드만으로 네비게이션 가능한지 확인 (Tab, Enter, Backspace)
- 스크린 리더에서 현재 위치가 올바르게 읽히는지 확인

### 크로스 브라우저 테스트
- Chrome, Firefox, Safari, Edge에서 동일하게 작동하는지 확인
- iOS Safari와 Android Chrome에서 테스트
