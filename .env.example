# Copy this file to .env and fill in your Gemini API key for local testing
# Do NOT commit your real API key to version control.
GEMINI_API_KEY=AIxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Optional: change model
# GEMINI_MODEL=gemini-2.5-flash-lite-preview-09-2025
# Proxy port (default 4000)
# PROXY_PORT=4000

# Google Cloud Text-to-Speech API Key
# Get this from Google Cloud Console: https://console.cloud.google.com/apis/credentials
# This should be the same API key as GEMINI_API_KEY if using the same Google Cloud project
GOOGLE_TTS_API_KEY=AIxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Proxy Server Configuration (Optional)
# If you need to use a proxy server to access Google Cloud TTS API, configure it here
# The proxy server will be used for all HTTPS requests to Google Cloud services
# Examples:
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# For authenticated proxies:
# HTTPS_PROXY=http://username:password@proxy.example.com:8080
# To exclude certain domains from proxying:
# NO_PROXY=localhost,127.0.0.1,.example.com

# Firebase Configuration
# Get these from Firebase Console: https://console.firebase.google.com
FIREBASE_API_KEY=your-firebase-api-key
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
FIREBASE_APP_ID=your-app-id
